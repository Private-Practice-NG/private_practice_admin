import { apiSlice } from "./apiSlice";
const USERS_URL = "/api/admins";

export const specialistApiSlice = apiSlice.injectEndpoints({
  endpoints: (builder) => ({
    
  }),
});

export const {  } = specialistApiSlice;
